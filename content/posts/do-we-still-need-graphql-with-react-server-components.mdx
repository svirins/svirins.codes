---
title: 'Do We'
summary: 'none'
publishedAt: '2024-01-01'
coverImage: '/assets/images/graphql.webp'
---
You should read this, it's **very important**.
You _might_ want to read this.
~~Never mind~~ - it wasn't _that_ important.

Let's try a few `combinations`:
**This text is strong, ~~this text is strong with strikethrough~~, and _this text is formatted with strong emphasis_**
***This text is formatted with strong emphasis too.***

# Heading 1
## Heading 2
### Heading 3
#### Heading 4
##### Heading 5
###### Heading 6

> Use quote blocks to emulate reply text.
> This line is part of the same quote.

```
action: function(ctx) {
    workflow.check(!ctx.issue.isChanged('votes'), workflow.i18n('Voting for a resolved issue is not allowed.'));
},
```

1. Fix usability problem
2. Clean up the page
   * Make the headings bigger
2. Push my changes
3. Create code review
   * Describe my changes
   * Assign reviewers
     * Ask for feedback

[inline link](https://www.jetbrains.com)
[inline link with tooltip](https://www.jetbrains.com "JetBrains: Development Tools for Professionals and Teams")
[reference link][1]


<StaticTweet id="1611745149788524544" />


<Image   
  alt={`Neon architecture diagram for connection pooling`}
  src={`/assets/images/music-for-programming.png`}
  width={800}
  height={600}
/>

<Callout emoji="💡">

Using WebSockets, instead of HTTP, does have tradeoffs. There might be additional latency on the first request setup, but subsequent requests are faster. There's an [RFC](https://www.rfc-editor.org/rfc/rfc9220.pdf) for WebSockets with `HTTP/3` which would remove that extra network roundtrip.

</Callout>


[source](https://hackteam.io/blog/do-need-graphql-now-react-server-components/)
